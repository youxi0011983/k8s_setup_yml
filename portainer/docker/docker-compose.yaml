version: '3.9'
services:
    portainer:
        image: portainer/portainer
        volumes:
            - ./portainer_data:/data
            - /var/run/docker.sock:/var/run/docker.sock
        restart: always
        container_name: portainer
        ports:
            - '8999:9000'
            - '8000:8000'